<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.contato.myapp2">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myapp2"
        tools:targetApi="31">

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Outras activities -->
        <activity
            android:name=".MeusContatosActivity"
            android:exported="true"
            android:label="Meus Contatos"/>

        <activity
            android:name=".NovoContatoActivity"
            android:exported="true"
            android:label="Novo Contato"/>

        <activity
            android:name=".AlterarContatoActivity"
            android:exported="true"
            android:label="Editar Contato"/>

        <activity
            android:name=".DeletarContatoActivity"
            android:exported="true"
            android:label="Deletar Contato"/>

    </application>

</manifest>
